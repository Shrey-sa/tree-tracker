name: Keep Render Alive

on:
  schedule:
    - cron: '*/14 * * * *'  # every 14 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend
        run: curl -s "${{ secrets.BACKEND_URL }}/api/schema/" > /dev/null && echo "✅ Render is awake"