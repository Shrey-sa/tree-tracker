# ── Django ────────────────────────────────────────
# Generate with: python3 -c "import secrets; print(secrets.token_hex(50))"
SECRET_KEY=REPLACE_WITH_GENERATED_KEY
DEBUG=False
ALLOWED_HOSTS=YOUR-EC2-PUBLIC-IP,localhost,backend

# ── Database ──────────────────────────────────────
DB_ENGINE=django.db.backends.postgresql
DB_NAME=treetracker
DB_USER=postgres
DB_PASSWORD=REPLACE_WITH_STRONG_PASSWORD
DB_HOST=db
DB_PORT=5432

# ── Redis ─────────────────────────────────────────
REDIS_URL=redis://redis:6379/0

# ── Email (AWS SES) ───────────────────────────────
# Fill these in after SES is set up
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
# EMAIL_HOST=email-smtp.ap-south-1.amazonaws.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=YOUR-SES-SMTP-USERNAME
# EMAIL_HOST_PASSWORD=YOUR-SES-SMTP-PASSWORD
# DEFAULT_FROM_EMAIL=TreeTracker <noreply@yourdomain.com>

# ── CORS ──────────────────────────────────────────
# After CloudFront is set up, replace with your CloudFront URL
CORS_ALLOWED_ORIGINS=http://YOUR-EC2-PUBLIC-IP

# ── AWS S3 for media uploads ──────────────────────
# Fill these in after S3 bucket is created
# USE_S3=True
# AWS_ACCESS_KEY_ID=YOUR-KEY
# AWS_SECRET_ACCESS_KEY=YOUR-SECRET
# AWS_STORAGE_BUCKET_NAME=treetracker-media
# AWS_S3_REGION_NAME=ap-south-1
